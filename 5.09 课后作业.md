1. 根据大纲完善自己的笔记

2. 使用同样的提示词，调整不同的 temp、top-p 等参数观察模型输出有什么不同（使用可调整该参数的对话工具进行测试）

3. 手动实现多轮对话，使用方舟大模型的 api 或者 ollama 的 api，如果你之前用的方舟的测试，现在请用 ollama 的 api 测试，调用方式为：
- apifox
- python requests
- 火山引擎 sdk（方舟大模型）

1. base_url 的概念一定要弄清楚，为什么要用 base_url，有什么好处

